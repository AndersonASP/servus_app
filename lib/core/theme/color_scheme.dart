import 'package:flutter/material.dart';

class ServusColors {
  // ===== Primárias =====
  static const primary = Color(0xFF4058DB);      // Azul principal
  static const primaryDark = Color(0xFF222F75);  // Azul escuro

  // ===== Secundária / Destaque =====
  static const secondary = Color(0xFFFFA07A);    // Laranja suave

  // ===== Light Mode =====
  static const background = Color(0xFFF9FAFB);   // Fundo geral
  static const surface = Color(0xFFFFFFFF);      // Cards, containers
  static const onSurface = Color(0xFF1F2937);    // Texto sobre superfícies claras
  static const textHigh = Color(0xFF222F75);     // Títulos
  static const textMedium = Color(0xFF4B5563);   // Subtítulos, descrições
  static const border = Color(0xFFE5E7EB);       // Linhas, bordas

  // ===== Dark Mode =====
  static const darkBackground = Color(0xFF0F1115); // Fundo escuro
  static const darkSurface = Color(0xFF1A1C20);    // Cards escuros
  static const onDarkSurface = Color(0xFFFFFFFF);  // Texto sobre superfícies escuras
  static const darkTextHigh = Color(0xFFFFFFFF);   // Títulos no dark
  static const darkTextMedium = Color(0xFF94A3B8); // Subtítulos, descrições no dark
  static const darkBorder = Color(0xFF2C2F36);     // Bordas discretas no escuro

  // ===== Feedback =====
  static const error = Color(0xFFFF7D7D);        // Erros / Alertas
  static const success = Color(0xFF43A047);      // Sucesso
  static const warning = Color(0xFFE0B200);      // Aviso

  // ===== Extras =====
  static const cardBackground = Color(0xFFDDE3FF);    // Cards personalizados
  static const funcoesBackground = Color(0xFF637DFF); // Cards de função
  static const funcoesBackgroundDark = Color(0xFF4058DB); // Cards de função
}