import 'package:flutter/material.dart';

class ServusColors {
  static const primary = Color(0xFF4058DB);      // Azul principal ajustado
  static const primaryDark = Color(0xFF222F75);  // Azul principal mais escuro
  static const secondary = Color(0xFFFFA07A);    // Amarelo (para avisos/destaques)
  static const background = Color(0xFFF9FAFB);   // Fundo claro
  static const surface = Color(0xFFFFFFFF);      // Containers e cartões
  static const textHigh = Color(0xFF1F2937);     // Títulos e texto forte
  static const textMedium = Color(0xFF4B5563);   // Texto secundário
  static const border = Color(0xFFE5E7EB);       // Linhas, divisórias
  static const error = Color(0xFFDC2626);        // Erros
}